import type { FilterFieldConfig } from "@/shared/types/filter.types";

export const CATALOGO_FILTROS_CONFIG: FilterFieldConfig[] = [
  {
    id: "name",
    label: "Nombre",
    type: "text",
    placeholder: "Ingrese el nombre",
  },
];
